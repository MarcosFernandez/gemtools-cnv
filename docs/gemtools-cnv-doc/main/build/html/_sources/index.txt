.. CNV PIPELINE documentation master file, created by
   sphinx-quickstart on Wed Apr 22 13:57:28 2015.
   You can adapt this file completely to your liking, but it should at least
   contain the root `toctree` directive.

============
CNV PIPELINE
============

--------------------
Copy number pipeline 
--------------------

Performs copy number calls based on Read Depth corrected by GC content in genome windows. Read Depth is calculated from **Whole Genome Sequencing (WGS)** data
aligned to a masked (Simple and Tandem repeats) reference genome.

Resources
=========

CNV GEMTOOLS PIPELINE
`````````````````````

Set of tools to perform all the pipeline. 

    `cnv gemtools pipeline`_ 

.. _cnv gemtools pipeline: ../../../doc/build/html/index.html


JIP GEM CNV PIPELINE
````````````````````

Use JIP pipeline system to run all the pipeline in three commands. (1. Assembly, 2. Pipeline Configuration, 3. Pipeline execution). 

    `jip cnv gemtools pipeline`_ 

.. _jip cnv gemtools pipeline: ../../../jip-doc/build/html/index.html


